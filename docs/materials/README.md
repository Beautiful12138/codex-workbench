# 轻量材料

这里存放外部或用户提供内容的搬迁/收集副本，例如导出的文档、会议纪要、接口摘录、外部资料摘要或临时上下文。

写入时尽量保留原始语义；如果是 Codex 基于项目、需求、代码或材料生成的梳理、解读、说明和总结，应放到 `docs/briefs/`。

本目录不表示需求已确认，也不替代 `docs/inbox/` 的 material / discovery / intake。需要进入正式需求流程时，再按 Workbench 规则登记和确认。
